<div class="rounded-2xl border border-zinc-200/90 bg-white/90 p-5 shadow-sm dark:border-zinc-700 dark:bg-zinc-900/60">
    <p class="text-xs font-semibold uppercase tracking-wider text-teal-600 dark:text-teal-400">{{ trans('live_showcase.props_label') }}</p>
    <p class="mt-2 text-base font-medium text-zinc-900 dark:text-white">{{ headline }}</p>
    <p class="mt-1 text-sm text-zinc-600 dark:text-zinc-400">{{ trans('live_showcase.props_tag') }}: <code class="rounded bg-zinc-100 px-1.5 py-0.5 text-xs dark:bg-zinc-800">{{ tag }}</code></p>
    <button type="button" live:click="rotateTag" class="mt-4 rounded-xl border border-teal-500/40 bg-teal-50 px-3 py-2 text-sm font-semibold text-teal-800 transition hover:bg-teal-100 dark:border-teal-500/30 dark:bg-teal-950/50 dark:text-teal-200 dark:hover:bg-teal-900/40">
        {{ trans('live_showcase.props_cycle') }}
    </button>
</div>
