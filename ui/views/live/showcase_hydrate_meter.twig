<div class="mt-6 rounded-2xl border border-violet-200/90 bg-white/90 p-5 shadow-sm dark:border-violet-900/50 dark:bg-zinc-900/60">
    <p class="text-xs font-semibold uppercase tracking-wider text-violet-600 dark:text-violet-400">{{ trans('live_showcase.meter_label') }}</p>
    <dl class="mt-3 grid gap-2 text-sm sm:grid-cols-2">
        <div>
            <dt class="text-zinc-500 dark:text-zinc-400">{{ trans('live_showcase.meter_hydrations') }}</dt>
            <dd class="tabular-nums text-lg font-semibold text-zinc-900 dark:text-white">{{ hydrations }}</dd>
        </div>
        <div>
            <dt class="text-zinc-500 dark:text-zinc-400">{{ trans('live_showcase.meter_pings') }}</dt>
            <dd class="tabular-nums text-lg font-semibold text-zinc-900 dark:text-white">{{ pings }}</dd>
        </div>
    </dl>
    <button type="button" live:click="ping" class="mt-4 rounded-xl bg-violet-600 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-violet-500">
        {{ trans('live_showcase.meter_ping') }}
    </button>
</div>
