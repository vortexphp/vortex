<div class="rounded-2xl border border-zinc-200/90 bg-white/90 p-5 shadow-sm dark:border-zinc-700 dark:bg-zinc-900/60">
    <p class="text-xs font-semibold uppercase tracking-wider text-amber-600 dark:text-amber-400">{{ trans('live_showcase.toggle_label') }}</p>
    <p class="mt-2 text-sm text-zinc-600 dark:text-zinc-400">
        {% if open %}
            {{ trans('live_showcase.toggle_on') }}
        {% else %}
            {{ trans('live_showcase.toggle_off') }}
        {% endif %}
    </p>
    <button type="button" live:click="toggle" class="mt-4 rounded-xl bg-amber-600 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-amber-500">
        {{ trans('live_showcase.toggle_btn') }}
    </button>
</div>
